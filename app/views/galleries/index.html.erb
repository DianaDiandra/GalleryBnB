<h1 class="mb-2 text-center text-black">Discover Our Artists</h1>
<p class="mb-4 text-center text-muted">
  Explore a diverse collection of creative talents—from actors and dancers to visual and makeup artists. Each profile highlights unique stories, artistic journeys, and featured work. Browse the gallery to get inspired and connect with the vibrant world of contemporary artistry.
</p>


<div class="container-fluid bg-white py-5 cards">
  <div class="row row-cols-1 row-cols-sm-2 row-cols-md-3 g-4 justify-content-center">
    <% @galleries.each do |gallery| %>
      <div class="col" style="max-width: 300px;">
        <div class="card border-0 shadow h-100 bg-white rounded-4 overflow-hidden">
          <% if gallery.image.attached? %>
            <%= image_tag url_for(gallery.image), class: "card-img-top", alt: gallery.artist, style: "height: 200px; object-fit: cover;" %>
          <% end %>
          <div class="card-body p-3">
            <h5 class="card-title mb-1"><%= gallery.artist %></h5>
            <p class="card-text mb-2"><%= gallery.description %></p>
            <small class="text-muted"><%= gallery.date.strftime("%B %d, %Y") if gallery.date %></small>
          </div>
        </div>
      </div>
    <% end %>
  </div>
</div>
